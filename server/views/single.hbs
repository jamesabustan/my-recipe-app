
<div class="card" style="width: 40rem;">
  <div class="card-body">
    <h5 class="card-title">{{name}}</h5>
    <h6 class="card-subtitle mb-2 text-muted">{{level}} : {{cook_time}}</h6>
    <a href="{{url}}" class="card-link">{{url}}</a>
  </div>
</div>

{{!-- Add button that lets user edit the specific ID recipe - must link to /recipe/{{id}}/edit Handlebar --}}

<a href="/recipe/{{id}}/edit" type="submit" class="btn btn-default btn-success">Edit</a>